<!DOCTYPE html>

<html>
<head>
    <title></title>
    <script src="@Url.Content("~/Scripts/jquery-1.5.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/modernizr-1.7.min.js")" type="text/javascript"></script>

    <script type="text/javascript">

        $(document).ready(function () {/*
            $.ajax({
                url: 'GetLastPaidDonations',
                success: function (data) {
                    for (var i in data) $('#lastPaidDonations').append('<div class="donatorPic"><img width="45" height="45" src="/Donation/DisplayImage/?donorId=' + data[i].DonorId + '" title="' + data[i].Name + '"/></div>');
                },
                error: function (xhr, status, error) {
                    $('#lastPaidDonations').append(xhr.responseText);
                }
            });
            $.ajax({
                url: 'GetTotalDonations',
                success: function (data) {
                    $('#totalDonations').append('<div class="blankLine"></div>');
                    $('#totalDonations').append('<div class="blankLine"></div>');
                    $('#totalDonations').append('<div class="blankLine"></div>');
                    $('#totalDonations').append('<div class="blankLine"></div>');
                    for (var i in data) $('#totalDonations').append('<img width="80" src="/Content/images/' + data[i].IconUrl + '" />&nbsp;&nbsp;&nbsp;' + data[i].Total + ' ' + data[i].UnitOfMeasure + '</div>');
                },
                error: function (xhr, status, error) {
                    $('#totalDonations').append(xhr.responseText);
                }
            });*/
        });
        
    </script>
</head>
<body>
    <div id="totalDonations" class="donatorWall" >
    @foreach (Link.BA.Donate.Models.TotalDonationsEntity item in ViewBag.TotalDonations)
    {
        <div class="centerDonationItem"><img width="80" src="@Url.Content("~/Content/images/" + item.IconUrl)" title="@(item.TotalCost) €" />&nbsp;&nbsp;&nbsp;@(item.Total-decimal.Truncate(item.Total)==0?item.Total.ToString("f0"):item.Total.ToString()) @item.UnitOfMeasure</div>
    }
    </div>
    <div id="lastPaidDonations" class="statusCallout" >    
        <div class="blankLine"></div>
        <div class="blankLine"></div>
        <div class="blankLine"></div>
        <div class="blankLine"></div>
        @foreach (Link.BA.Donate.Models.LastPayedDonationEntity item in ViewBag.LastPayedDonations)
        {
            <div class="donatorPic"><img width="45" height="45" src="/Donation/DisplayImage/?donorId=@item.DonorId" title="@item.Name"/></div>
        }
    </div>
</body>
</html>
